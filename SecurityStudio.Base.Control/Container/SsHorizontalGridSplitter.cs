using System.Windows;
using System.Windows.Controls;

namespace SecurityStudio.Base.Control.Container
{
    public class SsHorizontalGridSplitter : GridSplitter
    {
        public SsHorizontalGridSplitter()
        {
            Height = 5;
            Margin = new Thickness(0);
        }
    }
}