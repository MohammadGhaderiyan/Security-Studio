using System.Windows;
using DevExpress.Xpf.Bars;

namespace SecurityStudio.Base.Control.Bar
{
    public class SsBarManager : BarManager
    {
        public SsBarManager()
        {
            Margin = new Thickness(0);
        }
    }
}